form
  gap 0.75rem

  button
    font-size inherit
    line-height inherit
    min-width 6rem
    padding-inline 0.5rem
    padding-block 0.5rem
    background var(--background)
    color var(--highlight)
    